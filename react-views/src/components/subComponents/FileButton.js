import React, { Fragment } from 'react';

class FileButton extends React.Component {
  render() {
    return (
        <button className="btn btn-primary" type="button">Choose File(s)</button>
      )
  }
}

export default FileButton;